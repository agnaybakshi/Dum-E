#pragma once

constexpr char VISION_ARM_WIFI_SSID[] = "YOUR_WIFI_SSID";
constexpr char VISION_ARM_WIFI_PASSWORD[] = "YOUR_WIFI_PASSWORD";

// Replace these with addresses valid for your Wi-Fi subnet.
const IPAddress VISION_ARM_WIFI_STATIC_IP(192, 168, 137, 50);
const IPAddress VISION_ARM_WIFI_GATEWAY(192, 168, 137, 1);
const IPAddress VISION_ARM_WIFI_SUBNET(255, 255, 255, 0);
const IPAddress VISION_ARM_WIFI_DNS1(8, 8, 8, 8);
const IPAddress VISION_ARM_WIFI_DNS2(1, 1, 1, 1);
